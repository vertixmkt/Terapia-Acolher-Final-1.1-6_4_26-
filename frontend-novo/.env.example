# URL do backend
# Dev:  http://localhost:3000
# Prod: https://api.seu-dominio.com.br
VITE_API_URL=http://localhost:3000

# IMPORTANTE: Nenhum segredo deve estar aqui.
# A autenticação admin é feita via login no painel (POST /api/auth/admin/login).
# O JWT resultante fica apenas em sessionStorage — nunca no bundle JS.
